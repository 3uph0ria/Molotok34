@model IEnumerable<Molotok34.Models.Clients>

@{
    ViewBag.Title = "Клиенты";
}

@Html.Partial("~/Views/Shared/_AdminHeader.cshtml")
@Html.Partial("~/Views/Shared/_NavAdmin.cshtml")

<div class="col">
    <h3 class="text-center font-weight-bold py-4">Клиенты</h3>
    <div class="shadow">
        <div class="row p-4">
            @if (TempData["notice"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show w-100" role="alert">
                    <p class="m-0">@TempData["notice"]</p>
                </div>
                TempData["notice"] = null;
            }
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            ФИО
                        </th>
                        <th>
                            Эл. Почта
                        </th>
                        <th>
                            Телефон
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.FullName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Email)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Phone)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@Html.Partial("~/Views/Shared/_AdminFooter.cshtml")


